# Archivo KV para la pantalla de menú
# Autor: Juan David

# Hola Jona: Este archivo define la interfaz gráfica de la pantalla de menú.
# En Kivy, los archivos KV permiten separar la lógica (Python) de la interfaz (KV).
# Aquí definimos cómo se ve la pantalla de menú y qué ocurre cuando el usuario
# interactúa con los elementos (por ejemplo, al hacer clic en un botón).

<MenuScreen>:
    BoxLayout:
        orientation: "vertical"
        padding: 20
        spacing: 15

        # Título del juego
        Label:
            text: "BATALLA NAVAL"
            font_size: 32
            bold: True
            size_hint_y: 0.2

        # Botón para jugar sin registro
        Button:
            text: "1. Jugar sin registro"
            on_release:
                # Establecer valores predeterminados y navegar a la pantalla de juego
                app.root.get_screen("juego").ids.ancho_input.text = "10"
                app.root.get_screen("juego").ids.alto_input.text = "10"
                app.root.get_screen("juego").ids.naves_input.text = "5"
                app.root.current = "juego"

        # Botón para registrarse
        Button:
            text: "2. Registrarse"
            on_release:
                app.root.current = "registro"

        # Botón para iniciar sesión
        Button:
            text: "3. Iniciar sesión"
            on_release:
                app.root.current = "login"

        # Botón para ver puntuaciones
        Button:
            text: "4. Ver puntuaciones"
            on_release:
                app.root.current = "puntuaciones"

        # Botón para salir
        Button:
            text: "5. Salir"
            on_release:
                app.stop()
